// Instrument panel along the bottom of the forward window.
// Inner-group +Z faces the camera. Screens/gauges must face -Z (toward the
// camera) or they render as invisible backs / edge-on cylinders.
// NOTE: All 3D cockpit instruments have been removed. The visible console
// is now a CSS overlay in HUD.tsx, centered at the bottom with radar in middle.
export const CockpitConsole = () => {
  return null
}
